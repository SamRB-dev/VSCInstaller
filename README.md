# VSCInstaller

A simple VSCode installer script to save myself from going through hoops to install the official version on arch based systems.

## Usage

### Install Requirements

```shell
pip install -r requirements.txt
```

### Run Installer

```shell
python VSCInstaller.py
```

## Demo
